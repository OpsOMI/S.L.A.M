# Unique identifier for each client device (e.g., USB flash drive)
client_key: 8b1b171e-b0a6-4194-b33e-92245273f62e # Our custom id for each flash.

# Server connection details including name, host, and port
server_name: slam
server_host: localhost
server_port: 6666
tls_cert_path: "./certs/example/server.crt"

# Network and connection settings for client
timeout_seconds: 1
reconnect_retry: 3
